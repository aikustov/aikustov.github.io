/**
 * Page images. Imported statically so Astro re-encodes them at build time and
 * emits a set of sizes: the markup and the Schema.org block use the same
 * object, so the paths cannot drift apart.
 */
import portrait from '../assets/portrait.jpg';

export { portrait };
